#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>
int _printf(const char *format, ...);
#include <stdio.h>
int h_char(va_list num_arg);
int h_string(va_list num_arg);
#include <string.h>
#endif
